# configs/degradation_blur.yaml — cấu hình đánh giá suy giảm chất lượng do mờ (Gaussian blur).
# Áp dụng làm mờ với kernel_size và sigma cho trước lên tập test, KHÔNG huấn luyện lại model.
seed: 123

degradation:
  name: blur
  kernel_size: 7
  sigma: 2.0
